# Реальные креды никогда не коммитим
.env
.env.local
.mcp.local.json

# Если менеджер впишет ключ прямо в .mcp.json (вместо env-переменных) —
# раскомментируйте строку ниже, чтобы файл с ключом не попал в git:
# .mcp.json

# Прочее
node_modules/
.DS_Store
*.log
